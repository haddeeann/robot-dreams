// save all the changes on this branch

// push the changes to github

// build the build folder

// remove all the files except this one (der) and the build folder
console.log('hello matey');

// move all the folders from the build folder into the root

// delete the build folder

// check out the branch or make a new one called gh-pages

// save changes to gh-pages branch

// push the gh-pages to the gh-pages github